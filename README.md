# lu-re.de website

## Deployment

## Development

* run `npm install`
* run `npm run serve` to start development server

## Troubleshooting

* eleventy-plugin-sass might require to run `sudo xcode-select --reset`



